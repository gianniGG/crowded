<%= link_to 'New Mission', new_project_path %>

<% @projects.each do |prj| %>
  <h2>
    Company: <%= prj.company.name %>
    <h3>
      <%= link_to prj.name, project_path(prj) %>
    </h3>
    <h4>
      Mission Statement: <%= prj.mission_statement %>
    </h4>
  </h2>
<% end %>

